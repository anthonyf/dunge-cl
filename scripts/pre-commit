#!/bin/bash
# Pre-commit hook: ensure Lisp files are properly indented.
# Install: ln -sf ../../scripts/pre-commit .git/hooks/pre-commit

# Only check if .lisp files are staged
if git diff --cached --name-only | grep -q '\.lisp$'; then
    make check-fmt
fi
