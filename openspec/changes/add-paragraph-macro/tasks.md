## 1. Engine

- [ ] 1.1 Add `p` macro to engine.scm after `text` macro

## 2. Content Migration

- [ ] 2.1 Replace `(text ...) (text "")` pairs with `(p ...)` in content.scm

## 3. Verify

- [ ] 3.1 Run all tests and verify they pass
