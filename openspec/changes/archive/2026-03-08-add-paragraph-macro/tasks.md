## 1. Engine

- [x] 1.1 Add `p` function to engine.scm after `text` function

## 2. Content Migration

- [x] 2.1 Replace `(text ...) (text "")` pairs with `(p ...)` in content.scm

## 3. Verify

- [x] 3.1 Run all tests and verify they pass
